# Dengue Situational Dashboard - Python dependencies
# Install with:  pip install -r requirements.txt
streamlit>=1.44          # native OIDC auth: st.login / st.user
Authlib>=1.3.2           # required by Streamlit's st.login (Google sign-in)
pdfplumber>=0.11
pandas>=2.0
scipy>=1.10           # Rt estimation + forecasting (dengue_forecast.py)
altair>=5.0
pydeck>=0.9
matplotlib>=3.7
openpyxl>=3.1
