# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Streamlit
.streamlit/secrets.toml

# Local tooling / OS
.claude/
*.log
.DS_Store
Thumbs.db

# Temp / generated
lk_districts_raw.geojson
dengue_situational_*.xlsx
district_*.csv

# Unrelated local materials that live in this folder but are NOT part of the
# dashboard (IEC posters, screenshots, working docs). Only the dengue
# daily-update PDFs and the app code belong in the repo.
*.webp
*.docx
*.pptx
*.png

# ...but DO track the dashboard logo, whatever extension it has.
!cert_niid_logo.*
